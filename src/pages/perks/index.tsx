import { AnimatePresence } from "framer-motion";
import "./perks.scss";
const PerksPage = () => {
  return (
    <AnimatePresence>
      <section id="perks" className="perks-container"></section>
    </AnimatePresence>
  );
};

export default PerksPage;
